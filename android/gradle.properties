org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# AGP 9.0.1 warns that 36 is the highest compile SDK it was tested against,
# but a plugin dependency requires 37 (see app/build.gradle.kts). The bump is
# deliberate; silence the warning rather than let it drown out real ones.
android.suppressUnsupportedCompileSdk=37
